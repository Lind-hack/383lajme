ADMIN_SECRET=383admin

# Local-only placeholders so middleware's createServerClient constructs without
# throwing. Real values live in Vercel env vars. auth.getUser() returns an error
# (not a throw) with these, so pages render for local UI verification.
NEXT_PUBLIC_SUPABASE_URL=https://lzzsfobhfzxmbeydqbsw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=local-placeholder-anon-key
